name: CI Pipeline
 
on:
  push:
    branches: ["main"]
 
jobs:
  ci:
    name: Checkout → Build → Test
    runs-on: ubuntu-latest
 
    steps:
      # 1. Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v4
        
